{
    "tasks": [
      {
        "type": "cppbuild",
        "label": "C/C++: build active file",
        "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",  // Path to your g++.exe compiler
        "args": [
          "-fdiagnostics-color=always",  // Enable colored compiler output
          "-g",                            // Include debugging information
          "${file}",                       // Reference to the source code file
          "-o",                            // Output file indicator
          "${fileDirname}\\${fileBasenameNoExtension}.exe"  // Output file name
        ],
        "options": {
          "cwd": "${fileDirname}"  // Working directory (same as source code file)
        },
        "problemMatcher": [
          "$gcc"  // Placeholder for compiler error/warning handling (specific to your environment)
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "detail": "Task generated by Debugger."
      }
    ],
    "version": "2.0.0"
  }
  