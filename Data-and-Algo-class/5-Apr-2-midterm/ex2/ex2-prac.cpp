// find the output of the pseudocode

/*
x <- 50
y <- 100

POINTER ptr1, ptr2 = NULL
ptr1 = ADDRESSOF(x)
ptr2 = ADDRESSOF(y)

DISPLAY(x, y)
DISPLAY(ptr1, ptr2)
DISPLAY ADDRESSOF(x, y)
DISPLAY ADDRESSOF(ptr1, ptr2)
DISPLAY (*ptr1, *ptr2)

*ptr1 ← *ptr1 + y
ptr2 ← ptr1
y ← *ptr2 + x

DISPLAY(x, y)
DISPLAY(ptr1, ptr2)
DISPLAY ADDRESSOF(x, y)
DISPLAY ADDRESSOF(ptr1, ptr2)
DISPLAY (*ptr1, *ptr2)
*/

int main() {
    int x = 50;
    int y = 100;

    POINTER* ptr1 = nullptr;


}